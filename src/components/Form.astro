<summary id="contain">
    <form id="form" method="POST">
        <label>Name</label>
        <input class="field" type="text" name="name" placeholder="Jane Doe"/>
        <label>Email</label>
        <input class="field" type="text" name="email" placeholder="example@email.com"/>
        <label>Subject</label>
        <textarea class="field" name="subject" rows="10" placeholder="Inquiry about services (WIP)"/>
        <button id="form__btn">Submit</button>
    </form>
</summary>

<style lang="scss">
    #contain {
        max-width: 400px;
        margin: 0 auto;
    }
    #form {
        max-width: 100%;
        display: flex;
        flex-direction: column;
        border-radius: 15px;
        padding: 3% 10%;
        margin: 4% 0;
        background-color: var(--surface1);
        color: var(--text);
        box-shadow: 8px 8px 8px #00000096;
        .field {
            border-radius: 12px;
            padding: 1% 3%;
            margin: 2% 0;
            border: none;
        }
        &__btn {
            margin-top: 4%;
            align-self: center;
            border-radius: 10px;
            background-color: var(--surface1);
            color: var(--text);
            border:none;
            box-shadow: 0 2px 4px #00000096;
            transition: transform 0.2s ease-in-out;
            &:hover {
                transform: scale(1.1);
                filter: brightness(160%);
            }
        }
    }
</style>